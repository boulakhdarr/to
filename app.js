const tools = [
  ['jpg-to-pdf','JPG to PDF','Convert images into one clean PDF'],['word-to-pdf','Word to PDF','Make documents easy to share'],['powerpoint-to-pdf','PowerPoint to PDF','Presentations, ready for anywhere'],['excel-to-pdf','Excel to PDF','Spreadsheets in a polished format'],['html-to-pdf','HTML to PDF','Save a web page as a document'],['pdf-to-jpg','PDF to JPG','Turn pages into image files'],['pdf-to-word','PDF to Word','Bring text back into an editable file'],['pdf-to-powerpoint','PDF to PowerPoint','Reuse slides without starting over'],['pdf-to-excel','PDF to Excel','Extract tables from your PDF'],['pdf-to-pdfa','PDF to PDF/A','Archive files for the long term']
];
const names = Object.fromEntries(tools.map(tool => [tool[0], tool[1]]));
const app = document.querySelector('#app');
function layout(content){ app.innerHTML = content; window.scrollTo(0,0); }
function home(){ layout(`<section class="hero"><div><div class="eyebrow">PDF tools, thoughtfully made</div><h1>Make paperwork feel a little more <em>human.</em></h1><p class="hero-copy">Simple, private file conversion for the moments when a document just needs to be in the right format.</p></div><div class="hero-art" aria-hidden="true"><div class="paper-card"><b>YOUR<br>DOCUMENT</b><span></span><span></span><span></span><p>Ready to move forward.</p></div></div></section><section class="section"><div class="section-inner"><div class="section-head"><div><div class="eyebrow">The toolbox</div><h2>One place. Every format.</h2></div><p>Pick a tool and get on with your day.</p></div><div class="tools-grid">${tools.map((t,i)=>`<a class="tool-card" href="#/${t[0]}"><span class="tool-icon">0${i+1}</span><h3>${t[1]}</h3><small>${t[2]}</small></a>`).join('')}</div></div></section><section class="section trust-band"><div class="section-inner"><div><div class="eyebrow">Our point of view</div><h2>Useful beats flashy.</h2></div><div class="trust-list"><div><strong>Private by default</strong><p>Your files belong to you. We keep the experience clear about what happens to them.</p></div><div><strong>Calm by design</strong><p>No confusing dashboards or buried buttons. Just the next useful step.</p></div><div><strong>Made for real work</strong><p>Clean results for applications, reports, archives, and everyday admin.</p></div><div><strong>Free to explore</strong><p>Start with the tools you need, without creating an account.</p></div></div></div></section>`);}
function toolPage(slug){ const title=names[slug]; const isJpg=slug==='jpg-to-pdf'; layout(`<section class="page tool-page"><div class="eyebrow">Paperplane tool</div><div class="tool-header"><div><h1>${title}</h1><p class="page-intro">${tools.find(t=>t[0]===slug)[2]}. Drop a file below and let Paperplane handle the fiddly part.</p></div></div><div class="tool-shell"><div class="dropzone" id="dropzone"><div><div class="upload-icon">＋</div><h2>Drop your file here</h2><p>or choose one from your device</p><label class="button" for="file-input">Choose file</label><input id="file-input" type="file" hidden></div></div><div class="file-state" id="file-state"><div class="file-name" id="file-name"></div><p id="state-copy">Your file is ready.</p><button class="button" id="convert">${isJpg?'Convert to PDF':'Prepare conversion'}</button></div></div><p class="tool-note">${isJpg?'Images are processed in your browser. Nothing is uploaded. Supports JPG and PNG.':'This tool is being prepared for a secure browser-based workflow. The page is ready for the conversion service.'}</p><div class="legal-copy"><h2>How to use ${title}</h2><p>Choose a file, check that it is the right one, and start the conversion. Paperplane keeps the path short so you can get back to the work around the document.</p><h2>Good to know</h2><p>For sensitive material, always review the downloaded result before sharing it. File quality and layout can vary depending on the source document.</p></div></section>`); setupUpload(isJpg,slug);}
function setupUpload(isJpg,slug){const input=document.querySelector('#file-input'), drop=document.querySelector('#dropzone'), state=document.querySelector('#file-state'), name=document.querySelector('#file-name'); const select=file=>{if(!file)return; name.textContent=file.name; state.classList.add('visible'); drop.style.display='none'; document.querySelector('#convert').onclick=()=>{if(!isJpg){document.querySelector('#state-copy').textContent='This conversion service will be available soon.';return} const reader=new FileReader(); reader.onload=()=>{const image=new Image();image.onload=()=>{const pdf=new jspdf.jsPDF({orientation:image.width>image.height?'landscape':'portrait',unit:'px',format:[image.width,image.height]});pdf.addImage(image,'JPEG',0,0,image.width,image.height);pdf.save(file.name.replace(/\.[^.]+$/,'')+'.pdf');document.querySelector('#state-copy').textContent='Downloaded. Your image stayed in this browser.'};image.src=reader.result};reader.readAsDataURL(file)} }; input.onchange=()=>select(input.files[0]);['dragenter','dragover'].forEach(e=>drop.addEventListener(e,ev=>{ev.preventDefault();drop.classList.add('dragover')}));['dragleave','drop'].forEach(e=>drop.addEventListener(e,ev=>{ev.preventDefault();drop.classList.remove('dragover')}));drop.addEventListener('drop',ev=>select(ev.dataTransfer.files[0]));}
function infoPage(kind){const pages={about:['About Paperplane','Paperwork is part of nearly every day. Paperplane exists to make the small, necessary file tasks less distracting. We build focused tools that respect your time and your files.'],contact:['Contact us','Have a question, found something unclear, or want to share an idea? Email hello@paperplane.tools and we will get back to you.'],'privacy-policy':['Privacy policy','We collect only what is necessary to operate this website. Files selected for browser-based tools are processed locally and are not sent to our servers. We do not sell personal information.'],terms:['Terms of service','Use Paperplane lawfully and review your converted files before relying on them. Services are provided as-is while we continue improving them.'],'cookie-policy':['Cookie policy','Paperplane uses only the cookies and similar technologies needed for basic site operation and, where enabled, measurement. You can control cookies through your browser settings.']};const p=pages[kind];layout(`<section class="page"><div class="eyebrow">Paperplane</div><h1>${p[0]}</h1><p class="page-intro">${p[1]}</p><div class="legal-copy"><h2>A clear promise</h2><p>We aim to explain what our tools do, keep the interface straightforward, and treat your documents with care. This page is a plain-language starting point, not legal advice.</p><h2>Questions</h2><p>For questions about this page, contact hello@paperplane.tools.</p></div></section>`)}
function faq(){layout(`<section class="page"><div class="eyebrow">Helpful answers</div><h1>Questions, without the maze.</h1><div class="faq-item"><h3>Are my files uploaded?</h3><p>JPG to PDF runs in your browser. Other tools will clearly state how they process files as their secure workflows become available.</p></div><div class="faq-item"><h3>Do I need an account?</h3><p>No. Paperplane is designed for quick, occasional work without an account.</p></div><div class="faq-item"><h3>Which file types are supported?</h3><p>Each tool page lists its supported formats. JPG to PDF accepts JPG and PNG images.</p></div></section>`)}
function blog(){layout(`<section class="page"><div class="eyebrow">The paper trail</div><h1>Practical guides for digital paperwork.</h1><p class="page-intro">Short, useful notes for making documents easier to send, store, and use.</p><div class="article-list"><article class="article"><h3>How to make a PDF from images without losing order</h3><p>A simple way to prepare scans, receipts, or a photo sequence before sharing.</p></article><article class="article"><h3>PDF or PDF/A: which one belongs in your archive?</h3><p>Understanding the difference between a shareable document and a long-term record.</p></article><article class="article"><h3>A calmer checklist for sending important files</h3><p>Five quick checks that catch the most common document mistakes.</p></article></div></section>`)}
function route(){const slug=location.hash.slice(2)||'';if(!slug)return home();if(names[slug])return toolPage(slug);if(slug==='faq')return faq();if(slug==='blog')return blog();if(['about','contact','privacy-policy','terms','cookie-policy'].includes(slug))return infoPage(slug);home()}
function updateSeo(slug){const title=slug&&names[slug]?`${names[slug]} | Free Online PDF Tool`:slug==='blog'?'PDF Guides and Tips | Paperplane':slug==='faq'?'PDF Tools FAQ | Paperplane':slug==='about'?'About Paperplane PDF Tools':slug==='contact'?'Contact Paperplane PDF Tools':'Paperplane PDF Tools | Simple Online Converters';document.title=title;const description=document.querySelector('meta[name="description"]');if(description)description.content=slug&&names[slug]?`${names[slug]} online. Convert your files simply and securely with Paperplane PDF Tools.`:'Free online PDF tools for converting JPG, Word, PowerPoint, Excel, HTML and PDF files. Simple, private and easy to use.';}
const menuToggle=document.querySelector('.menu-toggle');
menuToggle.addEventListener('click',()=>{const open=document.body.classList.toggle('menu-open');menuToggle.setAttribute('aria-expanded',String(open));menuToggle.setAttribute('aria-label',open?'Close navigation':'Open navigation');});
document.querySelectorAll('.main-nav a,.header-link').forEach(link=>link.addEventListener('click',()=>{document.body.classList.remove('menu-open');menuToggle.setAttribute('aria-expanded','false');menuToggle.setAttribute('aria-label','Open navigation');}));
window.addEventListener('hashchange',route);updateSeo(location.hash.slice(2));route();window.addEventListener('hashchange',()=>updateSeo(location.hash.slice(2)));